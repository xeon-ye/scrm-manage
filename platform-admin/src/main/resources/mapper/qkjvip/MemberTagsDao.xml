<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.platform.modules.qkjvip.dao.MemberTagsDao">
    <delete id="deleteBatch">
        DELETE FROM qkjvip_member_tags WHERE tag_id IN
        <foreach item="tagId" collection="array" open="(" separator="," close=")">
            #{tagId}
        </foreach>
    </delete>

    <select id="queryTagsList" resultType="string">
		SELECT tag_id FROM qkjvip_member_tags WHERE member_id = #{value}
	</select>
</mapper>